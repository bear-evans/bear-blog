// ======================================
// Dashboard
// --------------------------------------
// Dashboard Blogroll, not to be confused with
// Dashboard Confessional.
// ======================================
const dash = (function () {
  function init() {}

  return {
    init: init,
  };
})();

// Trigger when document is ready
$(function () {
  dash.init();
});
